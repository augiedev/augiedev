<apex:page tabStyle="Account" controller="GoogleMapsExtensions" showHeader="false" sidebar="false">

<head>

<style>
#map {
  font-family: Arial;
  font-size:12px;
  line-height:normal !important;
  height:500px;
  background:transparent;
}
</style>

</head>
 
<body>
<div id="map"></div> 
</body> 

<script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"></script>
<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script> 
<script src="http://maps.google.com/maps?file=api&amp;v=2&amp;key=ABQIAAAAUPDbx9FDmE8VpIzoCGpsbRRbsXYB00TTPrJQhPTC8vyrXcyJDxQInUawKiAt3I45ix9mymVVrQMinw" type="text/javascript">

</script>
<script type="text/javascript"> 

accounts = new Array();
// Add HeadAccount to the Array
accLoc = getLocation("{!headAccount.BillingStreet}", "{!headAccount.BillingCity}", "{!headAccount.BillingPostalCode}", "{!headAccount.BillingCountry}");
accounts[accounts.length] = new account("{!headAccount.id}", "{!headAccount.Name}", "{!headAccount.BillingStreet}", "{!headAccount.BillingCity}", "{!headAccount.BillingPostalCode}", "{!headAccount.BillingCountry}", accLoc);
<apex:repeat value="{!zoneAccs}" var="acc">
   // Loop through all Accounts with the same zone and add them to the Array
   accLoc = getLocation("{!acc.BillingStreet}", "{!acc.BillingCity}", "{!acc.BillingPostalCode}", "{!acc.BillingCountry}");
   accounts[accounts.length] = new account("{!acc.id}", "{!acc.Name}", "{!acc.BillingStreet}", "{!acc.BillingCity}", "{!acc.BillingPostalCode}", "{!acc.BillingCountry}", accLoc);
</apex:repeat>

alert('Loc ' + accounts[0].location);
function account(id, name, street, city, postalCode, country, location) {
	this.id=id;
	this.name=name;
	this.street=street;
	this.city=city;
	this.postalCode=postalCode;
	this.country=country;
	this.location = location;
}

function getLocation(street, city, postalCode, country) {

   var geocoder = new google.maps.Geocoder();
   var address = street + ", " + city + ", " + postalCode + ", " + country;      
   geocoder.geocode( { address: address}, function(results, status) {
      if (status == google.maps.GeocoderStatus.OK && results.length) {
         if (status != google.maps.GeocoderStatus.ZERO_RESULTS) {     
            return results[0].geometry.location;
         }
      }
   });
}

function initialize() {
	
    var myOptions = {
       zoom: 15,
       mapTypeId: google.maps.MapTypeId.ROADMAP,
       mapTypeControl: false
    }
    // Create the map
    var map = new google.maps.Map(document.getElementById("map"), myOptions);
    var marker;
    var geocoder = new google.maps.Geocoder();
    for (i = 0; i < accounts.length; i++) {
	    var address = accounts[i].street + ", " + accounts[i].city + ", " + accounts[i].postalCode + ", " + accounts[i].country;
        var acc = accounts[i];
		geocoder.geocode( { address: address}, function(results, status) {
			if (status == google.maps.GeocoderStatus.OK && results.length) {
				if (status != google.maps.GeocoderStatus.ZERO_RESULTS) {
					acc.location = results[0].geometry.location;
					accounts[i] = acc;
					alert('Acc Loc: ' + accounts[i].location + '. Address: ' + address + ' AccName: ' + accounts[i].name);
				}
     		}
		});
	}
alert('Flength is: ' + accounts.length);
//accTest = accounts[0]; 
alert('FLoc is: ' + accounts[0].location);	

}     
     /* 
        if (i == 0) {
	        //center map
	        map.setCenter(results[0].geometry.location);
	    }
        
        //create marker
        marker = new google.maps.Marker({
            //position: results[0].geometry.location,
            position: headLocation,
            map: map,
            title: accounts[0].name
        });
        
        //add listeners
        google.maps.event.addListener(marker, 'click', function() {
          infowindow.open(map,marker);
        });
        
    	var infowindow = new google.maps.InfoWindow({
    		content: "<b>" + acc.name + "</b><br>" + acc.street + "<br>" + acc.city + ", " + acc.postalCode + "<br>" + acc.country + "<br/><a target='_blank' href='/" + acc.id + "'>LINK</a>"
  	  	});
        
        google.maps.event.addListener(infowindow, 'closeclick', function() {
          map.setCenter(marker.getPosition()); 
        });
        
      }
      
    } else {
  //    if (i == 0) {
      	$('#map').css({'height' : '15px'});
      	$('#map').html("Oops! " + acc.Name + "'s address could not be found, please make sure the address is correct.");
      	resizeIframe();
 //     }
    }
  });
  */
   //   }
   // resizeIframe();
   // alert('after resizeIFrame');


function resizeIframe() {
    alert('in resizeIFrame');
    var me = window.name;
    if (me) {
      var iframes = parent.document.getElementsByName(me);
      if (iframes && iframes.length == 1) {
        height = document.body.offsetHeight;
        iframes[0].style.height = height + "px";
      }
    }
  }

</script>
</apex:page>